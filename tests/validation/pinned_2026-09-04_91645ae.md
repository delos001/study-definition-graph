# Validation record: pinned

Written by `pytest --validation-report` on 2026-09-04 17:34 Eastern Daylight Time. Design and rationale for these tests: `tests/README.md`.

| | |
| --- | --- |
| Verdict | **PASS**: pytest exit status 0 (all tests passed) |
| Counts | 12 passed, 0 failed, 0 error, 0 skipped, in 7.3 s |
| Component | `src/sdg/pinned.py` |
| Code commit | `91645ae` (uncommitted changes present at run time) |
| Test file | `tests/test_pinned.py` sha256 `aa809ad2bbd7395dabeb35463f07f23aad06ff2269ac318447c39c513a289f0c` |
| Fixtures | `tests/fixtures/usdm_three_classes.yml` sha256 `50f0c08ce0356a6595871e89621497d93302f2d3a5aacf8ea2cdd63303a1aaf1` |
| Pinned USDM data | https://raw.githubusercontent.com/cdisc-org/DDF-RA/aa303cb32f5d3ceecc68a16803e26720d2c1fc26/Deliverables/UML/dataStructure.yml, sha256 `6f49a407aef41a66dd40a82ad3348672a22cd8874849254d7e4f27d6275daca6`, present |
| Command | `pytest --validation-report` |
| Run by | Jason Delosh |
| When | 2026-09-04 17:34:47 Eastern Daylight Time |
| Python / pytest | 3.12.13 / 9.1.1 |
| Platform | Windows-11-10.0.26200-SP0 |

| Test | Kind | Proves | Outcome |
| --- | --- | --- | --- |
| `test_running_from_inside_the_repo` | positive | require_repo() accepts this checkout: the folder the package takes to be the repo holds pyproject.toml and data/manifests/. | passed |
| `test_real_pinned_file_comes_back_with_its_identity` | positive | The pinned USDM model file verifies and comes back with the fingerprint and url its manifest records, and its content is readable. | passed |
| `test_string_and_path_name_the_same_file` | positive | pinned() accepts the repo-relative string a manifest writes or a full Path to the same file, and produces the same record for both. | passed |
| `test_recorded_fixture_verifies` | positive | A file whose manifest entry carries the right size and fingerprint comes back as a PinnedFile whose content reads correctly. | passed |
| `test_not_inside_the_repo_names_the_install_fix` | negative | When the package is not running from inside its repo (the folder it takes to be the repo has no pyproject.toml), pinned() refuses with a message that says where the package was found and gives the install command. | passed |
| `test_recorded_but_not_downloaded_raises_filenotfound` | negative | A file that a manifest records but that is not on disk raises FileNotFoundError, the same failure a missing download gives everywhere. | passed |
| `test_unrecorded_file_says_no_entry_records_it` | negative | A file no manifest entry records (this fixture, against the real manifests) is refused with a message saying exactly that, not with the fingerprint-mismatch remedy. | passed |
| `test_unreadable_manifest_says_unreadable` | negative | A manifest that is not valid JSON is reported as unreadable, with the remedy pointing at the manifests folder, not at re-downloading the file. | passed |
| `test_no_manifests_at_all_says_so` | negative | An empty manifests folder is reported as 'no manifests found', with the same restore remedy. | passed |
| `test_entry_without_sha256_says_malformed` | negative | A manifest entry for the file that carries no sha256 is reported as malformed, with the remedy pointing at that entry. | passed |
| `test_fingerprint_mismatch_shows_both_values_and_recovery` | negative | A recorded sha256 that differs from the file's is reported with both values, the manifest that records it, and the three recovery paths. | passed |
| `test_size_mismatch_is_reported_as_size` | negative | A recorded byte count that differs from the file's is reported as a size difference (checked before the hash, since it usually means a truncated download), with the same recovery paths. | passed |
